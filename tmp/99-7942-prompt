<Context>
You receive a selection in neovim that you need to replace with new code.
The selection's contents may contain notes, incorporate the notes every time if there are some.
consider the context of the selection and what you are suppose to be implementing
<SELECTION_LOCATION>
range(point(13,1),point(29,2))
</SELECTION_LOCATION>
<SELECTION_CONTENT>
impl DiscordAlerter {
    pub fn new(config: &AlertingConfig) -> Result<Self> {
        dotenv().ok();
        let webhook_env = env::var(config.webhook_env.clone());
        let webhook_url = match webhook_env {
            Ok(url) => println!("test"),
            Err(e) => println!("error"),
        };

        let client = reqwest::Client::new();

        Ok(DiscordAlerter {
            client,
            webhook_url: config.webhook_url.clone().unwrap(),
        })
    }
}
</SELECTION_CONTENT>
<FILE_CONTAINING_SELECTION>
use std::env;

use anyhow::Result;
use dotenv::dotenv;

use crate::{alert::AlertSender, config::configs::AlertingConfig};

pub struct DiscordAlerter {
    client: reqwest::Client,
    webhook_url: String,
}

impl DiscordAlerter {
    pub fn new(config: &AlertingConfig) -> Result<Self> {
        dotenv().ok();
        let webhook_env = env::var(config.webhook_env.clone());
        let webhook_url = match webhook_env {
            Ok(url) => println!("test"),
            Err(e) => println!("error"),
        };

        let client = reqwest::Client::new();

        Ok(DiscordAlerter {
            client,
            webhook_url: config.webhook_url.clone().unwrap(),
        })
    }
}

impl AlertSender for DiscordAlerter {
    fn send(&self, anomalies: &[crate::analysis::types::Anomaly]) -> anyhow::Result<()> {
        Ok(())
    }
}
</FILE_CONTAINING_SELECTION>

</Context>
<Prompt>
Can you fix the errors in this function?
</Prompt>

<Location><File>/Users/jadenhums/projects/infrawatch/src/alert/discord.rs</File><Function>range(point(13,1),point(29,2))</Function></Location>
<FunctionText>impl DiscordAlerter {
    pub fn new(config: &AlertingConfig) -> Result<Self> {
        dotenv().ok();
        let webhook_env = env::var(config.webhook_env.clone());
        let webhook_url = match webhook_env {
            Ok(url) => println!("test"),
            Err(e) => println!("error"),
        };

        let client = reqwest::Client::new();

        Ok(DiscordAlerter {
            client,
            webhook_url: config.webhook_url.clone().unwrap(),
        })
    }
}</FunctionText>
<TEMP_FILE>./tmp/99-7942</TEMP_FILE>
<MustObey>
NEVER alter any file other than TEMP_FILE.
never provide the requested changes as conversational output. Return only the code.
ONLY provide requested changes by writing the change to TEMP_FILE

never attempt to read TEMP_FILE.
It is purely for output.
Previous contents, which may not exist, can be written over without worry
After writing TEMP_FILE once you should be done.  Be done and end the session.

</MustObey>